# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index

POST    /auth/register                          UserController.Add
POST	/auth/login								UserController.Login
POST    /user/finished/:pid                     UserController.FinishedPaper
GET     /demo/db                                Demo.Database
GET     /users/:uid                             UserController.Fetch

POST    /question/add                           QuestionController.Add
POST    /question/excel                         QuestionController.AddFromExcel
POST    /question/:qid                          QuestionController.Fetch

GET     /news                                   NewsController.GetAll
GET     /news/:nid                              NewsController.GetOne
POST    /news/:uid                              NewsController.Save


GET     /paper/random                           PaperController.Random
POST    /paper/add                              PaperController.Add
GET     /paper/:pid                             PaperController.Fetch

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
